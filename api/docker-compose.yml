version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: nomeacao-db
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=nomeacao
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: